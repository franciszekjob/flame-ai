apiVersion: v1
kind: Service
metadata:
  name: litellm
  namespace: observability
  labels:
    app.kubernetes.io/name: litellm
    app.kubernetes.io/part-of: flame-ai
spec:
  selector:
    app.kubernetes.io/name: litellm
  ports:
    - name: http
      port: 4000
      targetPort: http
