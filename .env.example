# Copy to .env and fill in. `.env` is gitignored.
# The Gemini key is read by the in-cluster LiteLLM proxy, which routes
# grafana-llm-app requests to gemini-1.5-flash via the Google Gemini API.
GEMINI_API_KEY=AIza...
